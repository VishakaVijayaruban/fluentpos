# Copy to .env and fill in before running docker compose.
# Secret used to sign JWTs. Use a random string of at least 32 characters.
JWT_KEY=
# Password for the bundled Postgres container.
POSTGRES_PASSWORD=postgres
# Origin allowed by CORS (the Angular client).
CORS_URL=http://localhost:4200
